// app/type.ts
export interface RoomData {
  id: string;
  name: string;
  departement: string;
  status: boolean;
  available: string;
  floor: string;
  capacity: string;
  expired: string;
  fill: string;
  initialColor: string;
}